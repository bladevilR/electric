# 持仓量查询

目标：拿到官方持仓、已成交量、可调买入边界、可调卖出边界、产品类型、查询日期/月度。

建议页面/入口线索：`/pxf-js-outer-planmod/fsjyccl`

手工操作：

1. 普通 Chrome + CA 手工登录。
2. 手动进入持仓量查询页面。
3. 选择月份、执行日、产品类型等页面要求的条件。
4. 手动查询。
5. 优先导出 Excel/CSV/PDF 到 `files/`。
6. 如果只有小时级数据，不要手工扩成 96 点；保留页面原始口径。
