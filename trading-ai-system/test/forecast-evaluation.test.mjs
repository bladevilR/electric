import test from 'node:test';import assert from 'node:assert/strict';
import {evaluatePointForecast,evaluateQuantiles,evaluateEventProbability,groupEvaluationPairs} from '../lib/forecast-evaluation.mjs';
test('point metrics include MAE RMSE bias MASE and baseline skill',()=>{const r=evaluatePointForecast([{forecast:10,actual:12,baseline:15,naiveScale:4},{forecast:20,actual:18,baseline:16,naiveScale:4}]);assert.deepEqual({n:r.sampleCount,mae:r.mae,rmse:r.rmse,bias:r.bias,mase:r.mase,skill:r.skillVsBaseline},{n:2,mae:2,rmse:2,bias:0,mase:.5,skill:.2});});
test('quantile and event metrics are deterministic',()=>{const q=evaluateQuantiles([{actual:20,p10:10,p50:18,p90:30},{actual:40,p10:15,p50:25,p90:35}],[.1,.5,.9]);assert.equal(q.interval80.coveragePct,50);assert.equal(q.interval80.meanWidth,20);const e=evaluateEventProbability([{probability:.8,actualLabel:1},{probability:.7,actualLabel:0},{probability:.2,actualLabel:0}],{threshold:.5});assert.deepEqual([e.truePositive,e.falsePositive,e.trueNegative,e.falseNegative,e.recall],[1,1,1,0,1]);});
test('unknown regimes remain separate',()=>{const g=groupEvaluationPairs([{forecast:1,actual:1,heatRegime:null},{forecast:2,actual:3,heatRegime:'normal'}],['heatRegime']);assert.ok(g.unknown);assert.ok(g.normal);});
