import test from 'node:test';import assert from 'node:assert/strict';import {replaySettlementEconomics} from '../lib/economic-replay.mjs';
test('incomplete settlement keeps economics null',()=>{const r=replaySettlementEconomics({forecastRun:{targetTradingDate:'2026-01-01'},strategyTrace:{rows:[]},settlementFacts:[],tariffVersion:null});assert.equal(r.status,'insufficient_settlement_evidence');assert.equal(r.strategyCostYuan,null);assert.equal(r.savingVsDefaultYuan,null);});
test('default strategy and operator costs remain separate',()=>{const r=replaySettlementEconomics({forecastRun:{targetTradingDate:'2026-01-01'},strategyTrace:{rows:[{pointIndex:1,executed:true}]},settlementFacts:[{businessDate:'2026-01-01',pointIndex:1,actualLabelVersion:'settlement_final',baselineCostYuan:100,strategyCostYuan:80,actualOperatorCostYuan:90,perfectInformationLowerBoundYuan:70}],tariffVersion:'v1'});assert.equal(r.savingVsDefaultYuan,20);assert.equal(r.economicRegretYuan,10);});
