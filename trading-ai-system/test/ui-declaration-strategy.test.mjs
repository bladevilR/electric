import test from 'node:test';import assert from 'node:assert/strict';import {buildDeclarationStrategyModel} from '../ui/view-models/declaration-strategy-model.js';import {renderDeclarationStrategyView} from '../ui/views/declaration-strategy-view.js';
test('strategy page preserves five terms and MW MWh constraints',()=>{const m=buildDeclarationStrategyModel({});assert.equal(m.constraints.declarationPower.unit,'MW');assert.equal(m.constraints.energyBlockLimit.unit,'MWh');const html=renderDeclarationStrategyView({mode:'real'});['主动申报','缺省申报','用户日前出清电力','实际平均负荷','推荐申报'].forEach(label=>assert.match(html,new RegExp(label)));});
