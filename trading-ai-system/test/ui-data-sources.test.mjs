import test from 'node:test';import assert from 'node:assert/strict';import {buildDataSourcesModel} from '../ui/view-models/data-sources-model.js';import {renderDataSourcesView} from '../ui/views/data-sources-view.js';
test('data source model exposes all decision groups and truthful empty status',()=>{const model=buildDataSourcesModel({registry:{sources:[]},catalog:{fields:[]}});assert.deepEqual(model.groups.map(g=>g.id),['P0','P1','weather','supply-network','forecast-audit']);const html=renderDataSourcesView({mode:'real',dataSources:{},fieldCatalog:{fields:[{fieldId:'x',confirmationStatus:'code_supported'}]}});assert.match(html,/代码已支持，尚无非空实值/);assert.match(html,/页面原始表头/);assert.doesNotMatch(html,/数据已就绪/);});
