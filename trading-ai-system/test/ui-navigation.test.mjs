import test from'node:test';import assert from'node:assert/strict';import{NAV_ITEMS,renderNavigation}from'../ui/navigation.js';test('six views follow decision workflow',()=>{assert.deepEqual(NAV_ITEMS.map(x=>x.id),['data-sources','market-cockpit','price-forecast','declaration-strategy','history-review','model-governance']);assert.match(renderNavigation({activeView:'market-cockpit'}),/aria-current="page"/);});
