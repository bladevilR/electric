import test from 'node:test'; import assert from 'node:assert/strict'; import { mkdtemp, writeFile } from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path';
import { runPythonForecast, unavailablePythonForecast } from '../lib/python-model-runner.mjs';
test('python runner times out safely and exposes an honest fallback', async () => { const dir = await mkdtemp(path.join(os.tmpdir(),'python-runner-')), script = path.join(dir,'slow.cjs'); await writeFile(script, 'setTimeout(()=>console.log(JSON.stringify({ok:true})),1000)'); await assert.rejects(runPythonForecast({ pythonPath: process.execPath, scriptPath: script, modelPath: dir, snapshotPath: script, timeoutMs: 25 }), /python_model_timeout/); assert.equal(unavailablePythonForecast().status, 'candidate_unavailable'); });
