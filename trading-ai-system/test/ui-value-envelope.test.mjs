import test from'node:test';import assert from'node:assert/strict';import{normalizeValueEnvelope,formatValueEnvelope}from'../ui/value-envelope.js';test('zero is real and null is missing',()=>{assert.equal(formatValueEnvelope(normalizeValueEnvelope({value:0,unit:'MW'})).primary,'0');assert.equal(formatValueEnvelope(normalizeValueEnvelope({value:null,unit:'MW'})).status,'missing');});
