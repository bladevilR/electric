const STAGES=[['evidence','时点证据'],['load','负荷预测'],['price','价格分布'],['supplyNetwork','供给与网络'],['positionLimits','持仓与限额'],['objectiveConstraints','目标与硬约束'],['recommendation','推荐申报']];
export function buildStrategyTrace(input={}){const stages=STAGES.map(([id,title])=>{const data=input[id]||null,missingFields=id==='supplyNetwork'&&!data?['availableCapacityMw','sectionUtilizationPct']:[];return{id,title,status:data?'available':missingFields.length?'degraded':'unavailable',data,inputs:data?.inputs||{},missingFields,conclusion:{conclusionId:`${input.targetDate||'unknown'}:${input.pointIndex||'all'}:${id}`,summary:data?.summary||'证据不足',status:data?'supported':'degraded',inputRefs:data?.inputRefs||[],featureSnapshotId:input.snapshot?.featureSnapshotId||input.featureSnapshotId||null,forecastRunIds:input.forecastRunIds||[],modelVersions:input.modelVersions||[],formulaVersion:input.formulaVersion||null,constraintRefs:input.constraintRefs||[],warnings:data?[]:['source_evidence_missing']}}});return{targetDate:input.targetDate||null,pointIndex:input.pointIndex||null,stages,baselineRecommendationAllowed:stages.find(s=>s.id==='load').status==='available',executionAllowed:false,humanReviewRequired:true};}
