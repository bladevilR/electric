const WEATHER_FIELDS=['temperatureC','dewPointC','relativeHumidityPct','windU10Mps','windV10Mps','windSpeed10Mps','windDirection10Deg','precipitationAmountMm','totalCloudCoverPct','surfaceSolarRadiationJm2'];
export function validateWeatherSnapshot(p={}){const errors=[];if(!p.forecastIssuedAt)errors.push('forecast_issued_at_required');if(!p.targetDate)errors.push('target_date_required');if(!Array.isArray(p.rows))errors.push('rows_required');return{ok:!errors.length,errors};}
export function normalizeWeatherSnapshot(payload={},source={}){const v=validateWeatherSnapshot(payload);if(!v.ok)throw new Error(v.errors[0]);const capturedAt=payload.capturedAt||new Date().toISOString();const facts=[];for(const row of payload.rows){for(const fieldId of WEATHER_FIELDS){if(row[fieldId]===undefined)continue;facts.push({sourceId:source.sourceId,fieldId,businessDate:payload.targetDate,pointIndex:row.pointIndex,value:row[fieldId],eventTime:row.targetTime,forecastIssuedAt:payload.forecastIssuedAt,availableAt:payload.availableAt||payload.forecastIssuedAt,capturedAt,sourceRevision:payload.sourceRevision||payload.forecastIssuedAt});}}return{facts,warnings:[]};}
