const READY=new Set(['confirmed_visible','confirmed_export','captured_nonempty']);
export function normalizeSupplyNetworkSnapshot(payload={},catalog={}){const facts=[],blockedFields=[],warnings=[];for(const row of payload.rows||[]){for(const [fieldId,value] of Object.entries(row)){if(['pointIndex','eventTime'].includes(fieldId))continue;const def=(catalog.fields||[]).find(x=>x.fieldId===fieldId);if(!def||!READY.has(def.confirmationStatus)){blockedFields.push(fieldId);continue;}facts.push({sourceId:payload.sourceId,fieldId,businessDate:payload.businessDate,pointIndex:row.pointIndex,value,availableAt:payload.availableAt,capturedAt:payload.capturedAt||new Date().toISOString(),sourceRevision:payload.sourceRevision||payload.availableAt});}}return{facts,warnings,blockedFields:[...new Set(blockedFields)]};}
