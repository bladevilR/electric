import {buildPriceForecastModel} from '../view-models/price-forecast-model.js';
export function renderPriceForecastView(state={}){const m=buildPriceForecastModel(state.forecastReport||{});return `<section class="cockpit-view" data-view="price-forecast"><header><h2>价格预测</h2><span class="mode-identity">${state.mode==='demo'?'演示环境 · 模拟输入':'真实环境 · 时点数据'}</span></header><p>${m.labels.join(' · ')}</p><div class="role-grid">${m.series.map(s=>`<article><h3>${s.role}</h3><p>${s.run?'版本与数据可追溯':'— · 尚无可用预测运行'}</p></article>`).join('')}</div><p>P10 / P50 / P90 · 区间宽度 · 校准状态 · 尖峰概率 · 模型版本 · 特征快照 ID</p>${m.warnings.map(w=>`<p class="warning">${w}</p>`).join('')}</section>`;}
