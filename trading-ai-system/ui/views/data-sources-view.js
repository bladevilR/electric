import {buildDataSourcesModel} from '../view-models/data-sources-model.js'; import {renderFieldCatalogTable} from '../components/field-catalog-table.js';
export function renderDataSourcesView(state={}){const model=buildDataSourcesModel({registry:state.dataSources,catalog:state.fieldCatalog,coverage:state.coverage});return `<section class="cockpit-view" data-view="data-sources"><header><h2>数据源与质量</h2><span class="mode-identity">${state.mode==='demo'?'演示环境 · 模拟输入':'真实环境 · 时点数据'}</span></header><div class="source-groups">${model.groups.map(g=>`<article><h3>${g.id}</h3><strong>${g.items.length}</strong><span>个来源</span></article>`).join('')}</div>${renderFieldCatalogTable(model)}</section>`;}
