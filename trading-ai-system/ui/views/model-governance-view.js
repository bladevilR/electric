import {buildModelGovernanceModel} from '../view-models/model-governance-model.js';
export function renderModelGovernanceView(state={}){const m=buildModelGovernanceModel(state.governanceReport||{});return `<section class="cockpit-view" data-view="model-governance"><header><h2>模型治理</h2><span class="mode-identity">${state.mode==='demo'?'演示环境 · 模拟输入':'真实环境 · 时点数据'}</span></header><p>版本树 · Champion · Challenger · 特征清单 · 训练窗口 · 制品哈希 · 验证门槛 · 消融 · 审批 · 回滚 · 代码提交</p>${m.versions.map(v=>`<article><h3>${v.modelId||'候选模型'}</h3><p>${v.statusText}</p></article>`).join('')||'<p>当前没有候选模型；不会自动上线。</p>'}<button>记录人工评审决定</button></section>`;}
