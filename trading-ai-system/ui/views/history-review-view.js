import {buildHistoryReviewModel} from '../view-models/history-review-model.js'; import {renderReliabilityChart} from '../components/reliability-chart.js';
export function renderHistoryReviewView(state={}){const m=buildHistoryReviewModel(state.accuracyReport||{});return `<section class="cockpit-view" data-view="history-review"><header><h2>历史复盘</h2><span class="mode-identity">${state.mode==='demo'?'演示环境 · 模拟输入':'真实环境 · 时点数据'}</span></header><div role="tablist">${m.tabs.map(t=>`<button role="tab">${t.id}</button>`).join('')}</div><p>真实发布预测 · 历史时点重放 · 最终结算复盘</p><p>${m.metrics.join(' · ')}</p><p>临时/当前结果覆盖：${m.outcomeCoverage.temporary??'证据不足'}；最终结果覆盖：${m.outcomeCoverage.final??'证据不足'}</p>${renderReliabilityChart([])}<p>点位/峰谷/工作日/季节/高温/停机/阻塞场景</p></section>`;}
