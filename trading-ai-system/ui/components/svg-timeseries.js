const esc=(v)=>String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function renderSvgTimeseries({title='96点曲线',unit='',series=[]}={}){const points=series.flatMap(s=>s.points||[]).filter(p=>Number.isFinite(Number(p.value))), values=points.map(p=>Number(p.value)), min=Math.min(...values,0),max=Math.max(...values,1),range=max-min||1;return `<figure class="timeseries local-scroll" role="region" aria-label="${esc(title)}，单位 ${esc(unit)}"><figcaption><strong>${esc(title)}</strong> · ${esc(unit)}</figcaption><svg viewBox="0 0 960 260" role="img" aria-label="${esc(title)}"><title>${esc(title)}，单位 ${esc(unit)}</title>${series.map((s,i)=>`<polyline fill="none" stroke="${['#3b82f6','#ef4444','#10b981','#a855f7'][i%4]}" stroke-width="3" points="${(s.points||[]).filter(p=>Number.isFinite(Number(p.value))).map(p=>`${Number(p.pointIndex||1)*10},${230-(Number(p.value)-min)/range*200}`).join(' ')}"/>`).join('')}</svg><div class="chart-legend">${series.map(s=>`<span>${esc(s.label||s.fieldId)}（${esc(unit)}）</span>`).join('')}</div><details><summary>查看96点明细</summary><table><thead><tr><th>点</th><th>值（${esc(unit)}）</th></tr></thead><tbody>${points.map(p=>`<tr><td>${p.pointIndex}</td><td>${p.value}</td></tr>`).join('')}</tbody></table></details></figure>`;}
