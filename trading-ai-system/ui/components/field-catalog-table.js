import {renderStatusBadge} from './status-badge.js';
const COLUMNS=['数据域','来源页面/接口','页面原始表头','程序字段','业务含义','单位','数据类型','粒度','主键/关联键','必填性','空值规则','更新时间/延迟','历史深度','当前确认状态','证据与待办'];
const esc=(v)=>String(v??'—').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function renderFieldCatalogTable(model={}){return `<div class="local-scroll" role="region" aria-label="字段目录，可横向滚动" tabindex="0"><table><thead><tr>${COLUMNS.map(x=>`<th>${x}</th>`).join('')}</tr></thead><tbody>${(model.fields||[]).map(f=>`<tr><td>${esc(f.domain)}</td><td>${esc(f.sourceRoute||f.sourceId)}</td><td>${esc(f.rawHeader)}</td><td><code>${esc(f.fieldId)}</code></td><td>${esc(f.businessMeaning||f.description)}</td><td>${esc(f.unit)}</td><td>${esc(f.dataType)}</td><td>${esc(f.granularity)}</td><td>${esc(f.key)}</td><td>${esc(f.required)}</td><td>${esc(f.nullRule)}</td><td>${esc(f.latency)}</td><td>${esc(f.historyDepth)}</td><td>${renderStatusBadge(f.confirmationStatus)}</td><td><button data-evidence-ref="${esc(f.evidenceRef||f.fieldId)}">查看证据</button></td></tr>`).join('')}</tbody></table></div>`;}
