export const NAV_ITEMS=[['data-sources','基础数据与预测依据','来源、字段、预测与策略证据'],['market-cockpit','市场驾驶舱','供需、天气、机组与风险'],['price-forecast','价格预测','96点分布、基线与候选'],['declaration-strategy','申报策略','逻辑链、约束与人工复核'],['history-review','历史复盘','真实发布、重放与结算'],['model-governance','模型治理','版本、门槛、审批与回滚']].map(([id,label,description],index)=>({id,label,description,accessibleLabel:`切换六视图工作流第${index+1}步`}));export function renderNavigation(state={}){return`<nav class="cockpit-nav" aria-label="市场决策驾驶舱">${NAV_ITEMS.map(x=>`<button type="button" aria-label="${x.accessibleLabel}" data-cockpit-view="${x.id}"${state.activeView===x.id?' aria-current="page"':''}><strong aria-hidden="true">${x.label}</strong><small aria-hidden="true">${x.description}</small></button>`).join('')}</nav>`;}
