export const NAV_ITEMS=[['data-sources','数据源与质量'],['market-cockpit','市场驾驶舱'],['price-forecast','价格预测'],['declaration-strategy','申报策略'],['history-review','历史复盘'],['model-governance','模型治理']].map(([id,label])=>({id,label}));export function renderNavigation(state={}){return`<nav class="cockpit-nav" aria-label="市场决策驾驶舱">${NAV_ITEMS.map(x=>`<button data-cockpit-view="${x.id}"${state.activeView===x.id?' aria-current="page"':''}>${x.label}</button>`).join('')}</nav>`;}
