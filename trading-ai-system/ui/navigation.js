export const NAV_ITEMS=[['data-sources','数据与预测','多源对齐与精度复盘'],['market-cockpit','市场概览','供需形势与出清行情'],['price-forecast','价格预测','日前分时预测与分位数'],['declaration-strategy','申报策略','优化建议与合规约束'],['history-review','历史复盘','实况比对与损益核算'],['model-governance','预测方法','模型生命周期与评估']].map(([id,label,description])=>({id,label,description,accessibleLabel:`打开${label}页`}));
export function renderNavigation(state={}){return`<nav class="cockpit-nav" aria-label="主要功能">${NAV_ITEMS.map(x=>`<button type="button" aria-label="${x.accessibleLabel}" data-cockpit-view="${x.id}"${state.activeView===x.id?' aria-current="page"':''}><strong aria-hidden="true">${x.label}</strong><small aria-hidden="true">${x.description}</small></button>`).join('')}</nav>`;}

