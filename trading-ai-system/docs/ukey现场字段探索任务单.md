# UKey 现场字段探索任务单

更新时间：2026-09-03

执行环境：安装 CA/UKey 驱动、可登录江苏电力交易平台（JSPEC）的 Windows 电脑

代码基线：`main` 分支，至少包含提交 `4d08954`（支持读取同源 iframe、frame 和开放 Shadow DOM 中的可见表格）

## 1. 任务目标

在插有 UKey 的真实电脑上，对系统需要的 JSPEC 页面进行一次有清单的定向探索，形成完整、可复核的字段字典和页面操作记录。

本任务只负责确认真实页面结构和数据口径，不在证据不完整时猜测字段，不自动申报、下单、保存交易或绕过 UKey/CA 确认。探索结果将作为下一版采集器字段映射、跨页面合并和完整性校验的唯一依据。

## 2. 已确认事实

- JSPEC 业务表格可能位于门户页内嵌的同源 iframe/frame 中；旧采集器只检查顶层页面，因此曾出现“页面看得到、程序读不到”。
- 当前版本已经能够递归读取同源 iframe/frame 和开放 Shadow DOM 中的可见表格。
- 2026-09-02 现场截图证明，选择历史日期 `2026-08-24` 后，“用户日前出清”页面会返回 96 行数据，点击采集后系统提示“已采集 96 行”。
- “用户日前出清”已确认的真实表头为：`时间`、`出清电力`、`统一结算点电价临时结果`、`统一结算点电价最终结果`。
- 当前代码只保留该页面的时间和日前用户侧电价，遗漏了“出清电力”。这是字段建模遗漏，不是平台无法读取。
- 当前首页“96 点申报曲线对比”需要目标日缺省申报和历史实际负荷；仅采集用户日前出清价格，首页仍会显示等待真实数据。
- 当前系统以 `日期 + pointIndex（1～96）` 作为跨页面合并的基本键，但各页面真实日期口径、时间格式、单位和缺失值规则尚未全部现场确认。

### 2.1 2026-09-03 Windows 现场补充探索记录

本节记录 2026-09-03 在 Windows 真机、真实 UKey 登录态下完成的一轮只读探索。采集期间未读取或保存 Cookie、Token、UKey PIN、证书、私钥或账号密码，未点击“临时保存”“申报”“导入”“预览”等可能改变业务状态的操作。

运行环境与方法：

- Windows 将 TCP `5141～5240` 保留为排除端口，默认服务端口 `5177` 无法绑定；本轮临时使用 `127.0.0.1:5300` 启动本地服务，未修改仓库代码。
- 使用专用 Chrome 配置目录 `.browser/jspec-managed-profile` 和只监听本机的 CDP 端口 `127.0.0.1:9224`。
- 登录后确认同时存在平台门户页和“新一代电力交易平台”业务页。
- 首次全量慢采在进入 P0-1 后出现“用户API访问频率过高！”，系统按安全规则停止；之后改为每次只进入一个目标页面并延长等待时间。
- 页面多次显示“您当前没有该接口访问权限，请联系管理员授权后访问”。因此本轮能确认页面、路由和静态表头，但多个页面无法取得业务数值或进入明细层。

本轮结果：

| 页面编号 | 本轮确认的页面标题 | 本轮看到的真实表头/结构 | 行数与时间 | 本轮状态 |
|---|---|---|---|---|
| P0-1 | 用户侧96点电力申报 | `序号`、`指定时间（日期：时：分）`、`电力（MW）` | 96 点，`00:15`～`24:00` | 默认日期为 `2026-09-04`；查询 `2026-08-24` 后电力列仍为空；页面提示接口无权限并出现频率限制 |
| P0-2 | 用户侧96点电力缺省申报 | `序号`、`指定时间（日期：时：分）`、`电力（MW）` | 页面生成 96 点结构 | 页面可达，但本轮未取得缺省申报数值；仍出现频率限制 |
| P0-3 | 用户日前出清 | `时间`、`出清电力`、`统一结算点电价临时结果`、`统一结算点电价最终结果` | 96 点，`00:15`～`24:00` | 查询 `2026-08-24` 后后三列均为 `-`（各 96 个）；频率提示已消失，但接口无权限提示仍在 |
| P0-4 | 日前出清结果(公开) | 本轮未确认业务表头 | 未确认 | 路由和页面标题可达；正文数据未成功加载，采集器一度看到上一页残留表格，禁止将残留表头记为本页字段 |
| P0-5 | 实时出清结果(公开) | 本轮未确认业务表头 | 未确认 | 路由和页面标题可达；正文数据未成功加载，禁止使用残留表头 |
| P0-6 | 实时市场加权均价（公开） | 本轮未确认业务表头 | 未确认 | 路由和页面标题可达；正文数据未成功加载，禁止使用残留表头 |
| P0-7 | `demo`（96点日电量路由） | 列表层：`序号`、`电力用户名`、`户号`、`日期` | 0 行 | 只到达用户/日期列表层，未取得某户某日的 96 点明细；功率或电量口径仍未确认 |
| P0-8 | `demo`（日结算路由） | 列表层：`序号`、`月份`、`交易需求名称` | 0 行 | 只到达结算需求列表层，金额、状态和明细字段仍未确认 |
| P1-1 | 交易结果查询 | `时段类型`、`总交易量（日均）`、`最高价`、`最低价`、`加权价格`、`中位数价格` | 0 行 | 表头已确认，样例值、单位、方向、交易时间和状态仍未确认 |
| P1-2 | 滚动撮合限额查询 | 当前可见表头仍为 `时段类型`、`总交易量（日均）`、`最高价`、`最低价`、`加权价格`、`中位数价格` | 0 行 | 页面标题和路由已确认，但表头与限额用途不一致，可能是未加载完成或残留内容；不得据此建立限额字段映射 |
| P1-3 | `demo`（持仓量查询路由） | `序号`、`市场成员名称名称`、`交易单元名称`、`日期`、`时段`、`持仓量`、`年度月度电量`、`发电企业转让电量`、`能量块电量`、`保量保价电量` | 0 行 | 列表表头已确认；原页面确实显示重复的“名称名称”，暂按原文记录；单位和汇总口径仍未确认 |

本轮新增确认的空值与完整性规则：

- P0-1 的 96 个时间点可以完整生成，但业务值全部为空；“有 96 行”不能等同于“主动申报数据完整”。
- P0-3 的业务空值明确表现为 `-`；`2026-08-24` 的出清电力、临时价和最终价均为 `-` 时，必须判定为无业务值，不得接受为有效价格快照。
- 页面标题或路由成功变化，不代表业务表格已经加载；若仍显示前一页面表头，必须标记为残留内容并拒绝采集。
- P0-7、P0-8 当前首先返回列表层，后续采集器需要明确“列表页 → 只读明细页”的导航条件，不能把列表行误当作 96 点或结算明细。

本轮仍需现场补齐：

1. 使用具备相应只读接口权限的账号再次复核 P0-1～P0-8，尤其是 P0-3 的非空历史日。
2. 为 P0-1、P0-2 各确认至少一个非空日期，并记录 MW 数值类型、精度和空值规则。
3. 在 P0-7 进入某户某日的只读明细，确认 96 点列名、单位以及页面给出的是功率还是电量。
4. 在 P0-8 进入只读结算明细，确认金额、状态、结算周期和一日多行规则。
5. 对 P0-4～P0-6 等待接口权限和频率限制恢复后重新采集，只有表头与页面用途一致时才写入字段字典。
6. 对 P1-2 核实为何“限额查询”显示交易价格统计表头，排除 SPA 残留或接口失败后再定义可买量、可卖量字段。

## 3. 开始前准备

### 3.1 人员分工

- UKey 持有人：只负责插拔 UKey、完成登录、选择日期和确认页面，不向任何人提供 PIN、密码、Cookie、Token、证书或私钥。
- 现场探索人员：按本任务单逐页检查、记录和截图，不操作申报提交、交易确认、保存、删除、撤回等按钮。
- 开发人员：探索完成前不根据页面名称猜字段含义；收到证据后再实现字段映射和合并规则。

### 3.2 代码与运行环境

在另一台电脑的仓库目录执行：

```powershell
git switch main
git pull --ff-only origin main
git log -1 --oneline
```

确认最新提交包含本任务单和 `4d08954`。保留电脑上已有的未提交改动；若工作区不干净，不要覆盖、回滚或强制拉取，先记录 `git status -sb` 并暂停。

启动方式：

```powershell
.\run-system.ps1
```

若使用免安装交付包，则必须完整解压到新文件夹后双击 `启动系统.bat`，不要直接在压缩包内运行，也不要混用旧版本打开的 Chrome 数据窗口。

### 3.3 安全边界

- 不记录或截图 UKey PIN、密码、Cookie、Token、Authorization、证书内容、私钥、手机号等敏感信息。
- 不打开 Chrome 密码管理器、证书管理器或开发者工具的 Cookie/Storage 面板。
- 不点击任何“提交、确认申报、交易、保存、删除、撤回”按钮。
- 只读取页面上已经显示的数据；不得调用未公开接口、伪造请求或绕过平台权限。
- 截图若包含账号、单位或敏感业务数据，先脱敏；未经确认不得提交到公开 Git 仓库。

## 4. 每个页面必须记录的内容

每进入一个页面，都要完整记录以下项目。只记录“看见的事实”，没有看到就写“未确认”。

1. 左侧菜单的完整进入路径。
2. 页面加载完成后的完整 URL 和 hash 路由；URL 中若有票据或敏感参数，只记录去除敏感参数后的路径。
3. 页面标题、卡片标题和表格标题的原文。
4. 日期控件的标签、格式、默认日期、可选范围以及查询按钮文字。
5. 选择日期并查询后，页面是否刷新、局部刷新或打开新页。
6. 表格每一列表头的精确原文、左右顺序和单位；包括括号、空格、换行和“临时/最终”等后缀。
7. 每列至少三条脱敏样例值，并标记数据类型：日期、时间、整数、小数、文本、百分比、金额或空值。
8. 表格总行数；96 点页面必须核对首行、末行、时间间隔、是否包含 `24:00`、是否分页或虚拟滚动。
9. 空值的真实表现，例如 `-`、`--`、空白、`null`、`0`；确认临时值和最终值的优先关系。
10. 功率、电量和电价的单位，尤其区分 `MW`、`MWh`、`kWh`、`元/MWh` 和 `元`。
11. 页面是否位于 iframe/frame；若能安全查看 DOM，只记录 frame 层级和业务页 URL，不读取存储或凭证。
12. 是否有“导出”按钮；如果允许导出，只导出查询结果并记录文件名、工作表名、列名和行数。
13. 点击“采集并校验当日数据”后的完整中文提示、采集行数，以及系统哪个页面发生变化。
14. 同一页面至少选择两个可用日期复核：一个已知历史日期和另一个可用日期。若只能查到一个日期，记录平台限制。

## 5. P0 必查页面

以下页面全部完成，才算本次字段探索完成。路由是当前代码中的定位线索，不代表真实页面已经验收；现场 URL 不一致时，以实际页面为准并记录差异。

| 编号 | 页面 | 当前路由线索 | 当前预期用途 | 现场必须确认的重点 | 当前状态 |
|---|---|---|---|---|---|
| P0-1 | 用户侧96点主动申报 | `/pxf-spotgoods-province-extranet/userBid96/index` | 历史主动申报曲线 | 真实表头、申报功率/电量口径、单位、96点时间 | 待现场完整确认 |
| P0-2 | 用户侧96点缺省申报 | `/pxf-spotgoods-province-extranet/userDefaultBid96/index` | 目标日申报基线 | 真实表头、缺省值口径、单位、日期与96点完整性 | 待现场完整确认 |
| P0-3 | 用户侧日前出清 | `/pxf-spotgoods-province-extranet/Dd2jyUserClearingResult/Dd2jyRqClearing` | 用户侧出清电力和日前价格 | “出清电力”的单位与含义；临时价/最终价空值和优先规则 | 表头已确认，字段映射不完整 |
| P0-4 | 日前公开出清 | `/pxf-spotgoods-province-extranet/afterDiscloseInformation/xrdClearingResultOnlyJiesuan/DayClearingResult` | 日前公开价 | 真实价格列名、节点/统一结算点口径、日期和96点 | 待现场完整确认 |
| P0-5 | 实时公开出清 | `/pxf-spotgoods-province-extranet/afterDiscloseInformation/xrdClearingResultOnlyJiesuan/CurClearingResult` | 实时节点/出清价格 | 当前值与历史值的区别、价格列和时间粒度 | 待现场完整确认 |
| P0-6 | 实时加权均价公开 | `/pxf-spotgoods-province-extranet/realTimeClearingRelease/RealTimeMarAvePricePublic` | 实时市场加权均价 | 表头、单位、是否支持历史日期、是否持续更新 | 代码有预期，待现场复核 |
| P0-7 | 96点日电量查询 | `/pxf-js-outer-deferrableload/dayElectricity` | 实际负荷/实际电量 | 页面究竟提供功率还是电量；单位；能否转换为同口径96点负荷 | 待现场完整确认 |
| P0-8 | 日结算查询 | `/pxf-js-outer-deferrableload/settleDay` | 日结算金额与复盘 | 金额字段、结算状态、日期口径、是否一日一行或多条明细 | 待现场完整确认 |

### P0-3 已知表头的专门核验

“用户侧日前出清”必须逐项确认：

- `时间` 是否始终为 15 分钟粒度；
- `出清电力` 的单位是否为 MW，是否代表成交/出清而不是申报；
- `统一结算点电价临时结果` 的单位；
- `统一结算点电价最终结果` 的单位；
- 最终结果为 `-` 或空白时是否应回退到临时结果；
- 最终结果出现后临时结果是否仍保留；
- 查询不同历史日期时四列是否都会变化；
- 96 行是否完整，首点和末点分别是什么。

在含义确认前，不得把“出清电力”映射成“主动申报”或“缺省申报”。后续代码应使用独立字段，例如 `clearedPower`，最终名称以现场口径为准。

## 6. P1 补充页面

P0 完成且现场时间允许时，再探索以下页面：

| 编号 | 页面 | 当前路由线索 | 需要确认 |
|---|---|---|---|
| P1-1 | 能量块成交结果 | `/pxf-trade-auction-extranet/myTransaction/TradeResult` | 成交电量、价格、方向、交易时间和状态字段 |
| P1-2 | 能量块可买可卖量/限额 | `/pxf-trade-auction-extranet/myTransaction/QuotaQuery` | 可买量、可卖量、单位、适用日期和时段 |
| P1-3 | 持仓量查询 | `/pxf-js-outer-planmod/fsjyccl` | 持仓字段、方向、单位、日期/周期和汇总口径 |

P1 页面只读。遇到交易、申报或确认入口立即停止，不为补字段进入可写流程。

## 7. 现场记录模板

每个页面复制一份下面的模板填写：

```text
页面编号：
页面名称：
执行时间：
执行人：
菜单路径：
脱敏后的页面路由：
页面/表格标题：
查询日期一：
查询日期二：
日期控件与查询行为：
是否位于 iframe/frame：
frame 层级或业务页路由：
完整表头（按顺序）：
各列单位：
各列业务含义：
脱敏样例值：
总行数：
首行：
末行：
分页/虚拟滚动情况：
空值形式：
导出文件名、工作表名、行列数：
采集提示全文：
采集后系统变化：
截图文件名：
仍未确认的问题：
```

## 8. 字段字典结果表

现场完成后，在本节追加结果。`程序字段` 不确定时留空，不得现场猜名。

| 页面编号 | 页面原始表头 | 单位 | 样例类型 | 业务含义 | 程序字段 | 是否必填 | 空值规则 | 已复核日期 |
|---|---|---|---|---|---|---|---|---|
| P0-1 | 序号 | 无 | 整数 | 96 点序号 | `pointIndex` | 是 | 不应为空 | 2026-08-24、2026-09-04 |
| P0-1 | 指定时间（日期：时：分） | 时间 | 时间 | 96 点时刻 | `timePoint` | 是 | 不应为空 | 2026-08-24、2026-09-04 |
| P0-1 | 电力（MW） | MW | 小数或空白 | 用户侧96点主动申报电力 | `declarationPower` 候选来源 | 是 | 本轮为输入空白；96 点全空必须拒绝 | 2026-08-24、2026-09-04 |
| P0-2 | 序号 | 无 | 整数 | 96 点序号 | `pointIndex` | 是 | 不应为空 | 2026-09-04 |
| P0-2 | 指定时间（日期：时：分） | 时间 | 时间 | 96 点时刻 | `timePoint` | 是 | 不应为空 | 2026-09-04 |
| P0-2 | 电力（MW） | MW | 小数或空白 | 用户侧96点缺省申报电力 | `defaultDeclarationPower` 候选来源 | 是 | 本轮未取得非空值；96 点全空必须拒绝 | 2026-09-04 |
| P0-3 | 时间 | 待确认 | 时间 | 96点时刻 | `timePoint` | 是 | 待确认 | 2026-08-24 |
| P0-3 | 出清电力 | 待确认 | 小数或 `-` | 待现场确认 | 待定，禁止映射为申报 | 是 | 本轮 96 行均为 `-`，按无业务值处理 | 2026-08-24、2026-09-04 |
| P0-3 | 统一结算点电价临时结果 | 待确认 | 小数或 `-` | 临时日前价格 | `dayAheadUserPrice` 的候选来源 | 条件必填 | 本轮 96 行均为 `-`；不得解析为 0 | 2026-08-24、2026-09-04 |
| P0-3 | 统一结算点电价最终结果 | 待确认 | 小数或 `-` | 最终日前价格 | `dayAheadUserPrice` 的优先来源 | 条件必填 | 本轮 96 行均为 `-`；不得解析为 0 | 2026-08-24、2026-09-04 |
| P0-7 | 序号 | 无 | 整数 | 用户/日期列表序号，不是96点序号 | 不映射 | 列表层必填 | 本轮无列表行 | 未取得可用日期 |
| P0-7 | 电力用户名 | 待确认 | 文本 | 96点日电量查询对象 | 待定 | 待确认 | 本轮无列表行 | 未取得可用日期 |
| P0-7 | 户号 | 待确认 | 文本 | 用电户标识 | 待定 | 待确认 | 本轮无列表行 | 未取得可用日期 |
| P0-7 | 日期 | 日期 | 日期 | 明细所属日期 | `date` 候选来源 | 是 | 本轮无列表行 | 未取得可用日期 |
| P0-8 | 月份 | 月 | 文本或日期 | 结算所属月份 | 待定 | 待确认 | 本轮无列表行 | 未取得可用日期 |
| P0-8 | 交易需求名称 | 无 | 文本 | 结算需求/批次名称 | 待定 | 待确认 | 本轮无列表行 | 未取得可用日期 |
| P1-1 | 时段类型 | 待确认 | 文本 | 成交统计时段类型 | 待定 | 待确认 | 本轮无业务行 | 未取得可用日期 |
| P1-1 | 总交易量（日均） | 待确认 | 小数 | 日均成交量 | 待定 | 待确认 | 本轮无业务行 | 未取得可用日期 |
| P1-1 | 最高价 / 最低价 / 加权价格 / 中位数价格 | 待确认 | 小数 | 成交价格统计 | 待定 | 待确认 | 本轮无业务行 | 未取得可用日期 |
| P1-3 | 日期 / 时段 | 日期、待确认 | 日期、文本 | 持仓所属日期和时段 | 待定 | 是 | 本轮无业务行 | 未取得可用日期 |
| P1-3 | 持仓量 | 待确认 | 小数 | 当前持仓量 | 待定 | 待确认 | 本轮无业务行 | 未取得可用日期 |
| P1-3 | 年度月度电量 / 发电企业转让电量 / 能量块电量 / 保量保价电量 | 待确认 | 小数 | 持仓构成 | 待定 | 待确认 | 本轮无业务行 | 未取得可用日期 |

## 9. 证据文件要求

每个 P0 页面至少保留：

- 一张包含页面标题、所选日期、完整表头和部分数据行的截图；
- 一张包含最后一行或总行数的截图；
- 一张采集完成提示截图；
- 若平台允许，保存一份原始导出文件，不修改列名和格式；
- 一份按第 7 节填写的文字记录。

建议文件名：

```text
P0-3_用户侧日前出清_2026-08-24_表头.png
P0-3_用户侧日前出清_2026-08-24_末行.png
P0-3_用户侧日前出清_2026-08-24_采集提示.png
P0-3_用户侧日前出清_2026-08-24_原始导出.xlsx
```

原始证据先保存在现场电脑的受控目录，不要直接提交 Git。只有完成脱敏检查并得到数据所有者许可后，才能将脱敏证据加入仓库。

## 10. 完成标准

同时满足以下条件，才可以报告“现场探索完成”：

- P0-1 至 P0-8 全部有记录；无法访问的页面必须有明确原因和页面证据，不能留空。
- 每个96点页面都核对了真实表头、单位、首末时点、总行数、空值和至少两个可用日期。
- 明确区分主动申报、缺省申报、出清电力、实际负荷/电量，不互相代用。
- 明确临时价格和最终价格的优先规则。
- 明确每个首页必需字段来自哪个页面，并能用 `日期 + pointIndex` 对齐。
- 记录每个页面是否位于 iframe/frame、是否分页或虚拟滚动。
- 保留脱敏截图和允许获取的原始导出；没有读取或泄露任何凭证。
- 用当前版本执行一次采集，记录“程序实际保存字段”与“页面真实字段”的差异。
- 将第 7、8 节填写完整，列出仍需开发修复的差异清单。

## 11. 探索完成后的开发任务输入

现场探索结果交回后，下一版开发至少需要处理：

1. 为“出清电力”建立独立字段，贯通解析、白名单校验、历史存储和展示。
2. 按页面定义必需字段；字段不全时返回“部分采集”，不得仅因存在日期和点位就提示成功。
3. 将不同页面的数据按 `日期 + pointIndex` 合并，同时保留字段来源、采集时间和页面路由。
4. 分开展示“历史申报”“出清电力”“实际负荷”，禁止以错误业务名称展示数据。
5. 只有目标日缺省申报和所需历史实际负荷达到完整性门槛时，才生成96点申报建议。
6. 为每个真实表头增加回归测试，并增加“页面有96行但关键字段缺失”的失败测试。

本任务不直接授权修改生产数据、提交交易或部署生产系统。开发改造、重新打包和现场验收应作为后续独立任务执行。
